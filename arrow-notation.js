const functionName = function () {
    //general function
    //this(it gives data universal. i.e gives data into outside the brackets.)
}


const nameFunction = ()=> {
    //arrow function
    //this(it gives data only within the bracket)
}

const singlereturn = () => {
    return true;
}

const singlereturnFunction = () => true;



